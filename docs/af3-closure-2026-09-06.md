# AF3 native-precision closure

Status: **completed for the nine-case, common-runtime, fixed-kernel panel**.
This is the first model under the
[upstream-first contract](precision-selection-protocol-2026-09-06.md).
Native BF16/operator policy is retained. One sampler multiplication's operand
order is restored to match the publisher's compiled graph; there is no precision
lowering. This is not publisher dependency-lock reproduction, independent
default-autotuning reproducibility, universal chemistry validation or a release.

## Completed results

### Current-tree recheck, 2026-09-08

Job 592 reran the current FoldJAX default path on 1UBQ, five samples, against
the preserved native audit and matched kernel decisions. Source snapshot:
`af3-current-source-20260908-lwjMbc`; artifacts:
`af3-current-result-20260908-zYQwpE/{foldjax,comparison.json}`.
Coordinates of all five samples, all raw/extracted confidence, independent
features, parameter manifests and observed RNG records match. Entity A's
Kabsch residual maximum is 6.415250900883943e-15 angstrom (identical coordinates).

The full comparator intentionally reports **passed=false**, exit 1. Four
checks fail: config.json, effective-config.json, cpp_sha256 and shared generated
assets. Existing config values are identical; candidate adds default
`foldjax_return_representations=[]` and `foldjax_stop_after="full"`. The current
source-keyed C++/CCD build differs in hash from the historical native build.
Neither these differences nor a single numerically exact case may be silently
waived into full admission. Next distinguish the explicit default-only config
extension from scientific settings and establish a fresh common-runtime native
reference for the current generated artifacts. Do not mutate old manifests.

Verification: queued capture exit 0; comparator exit 1 for the four checks
above; numerical and RNG gates pass. The earlier current-tree CPU check passed
65 AF3 tests, and 594 historical panel artifact hashes were rechecked intact.
No current-tree nine-case completion, uninstrumented benchmark, release or
push is claimed by this recheck.

Fresh native job 593 uses unmodified publisher Python with the current shared
runtime and identical fixed kernels, under the same result root's `native/`.
It completed successfully. `current-comparison.json` now passes every check
except config.json/effective-config.json: current C++/CCD hashes match, as do
all five coordinates, raw/extracted confidence, inputs, parameters and RNG.
The only configuration differences are the two default-only FoldJAX fields
above; no shared field differs. A new comparator diagnostic records left-only,
right-only and changed fields while preserving the exact-equality gate.
Verification: comparator remains passed=false (exit 1), not silently waived;
42 comparator tests and scoped Ruff pass. Any explicit semantic-config
admission policy remains separate from this raw equality result. This is still
only the current-tree 1UBQ audit, not the complete nine-case/public-mode gate.

Job 594 expands the current-tree check to protein–ligand 5SAK, n=5, with
fresh native/FoldJAX audit arms under the same immutable source as 592/593.
It reuses recorded compatible kernel choices with strict XLA coverage; output
root `af3-current-5sak-20260908-mIDVLV/protein_ligand_5sak/audit`. The driver
runs both arms then the unchanged strict comparator; default-only config
differences are expected to remain explicit, not waived. Native is currently
running; no numerical result is claimed yet.
Verification: 107 comparator/AF3/session tests plus 3 capture tests passed.
An initial command referenced nonexistent `tests/test_af3_closure_report.py`
and collected no tests; the corrected commands used the actual test files.

Job 594 completed both inference arms successfully. Its driver exits 1 because
the final strict comparator rejects config.json/effective-config.json; every
other check passes, including raw/extracted confidence, observed tape, inputs,
parameters, C++/CCD identity and compiler policy. All five coordinate arrays
are exactly equal. Per-entity fitted maxima: protein A
1.9883543175048644e-14 and ligand L 1.1096994001486553e-14 angstrom. These are
alignment roundoff, not structural drift. This is current-tree 5SAK numerical
parity, not a full strict configuration pass or performance measurement.
Verification: both finished artifacts present, completed comparison inspected;
queue exit 1 retained as the comparator outcome, not mislabeled inference failure.

Current-source protein–RNA 1URN pair is queued as job 595, output
`af3-current-1urn-20260908-DC5lGe/protein_rna_1urn/audit`. Both arms use the
same source snapshot/common runtime as 592–594, n=5 and recorded compatible
kernel choices. No new chemistry, sampling or tolerance setting is introduced.
The current-tree audit coverage is now 1UBQ and 5SAK numerically verified,
1URN pending; the historical nine-case panel remains separate. Verification:
input and XLA cache paths exist, GPU checked free before queue submission.
No 1URN result, uninstrumented timing or complete current-tree panel is claimed.

Job 595 completed both arms: all five protein–RNA coordinate pairs are exactly
equal, raw confidence is bitwise equal, and extracted confidence, input,
parameter, RNG and runtime/compiler provenance checks pass. Fitted entity
maxima are P 7.813083125640918e-15 / R 9.576138671688784e-15 angstrom.
Only config.json and effective-config.json fail, as in 1UBQ/5SAK; driver exit 1
is the strict comparator outcome, not an inference failure. Current-tree
numerical audit coverage is three cases / 15 paired samples, not nine cases.
Verification: finished artifacts and completed report inspected. For later
3GCA replay, the historical aggregate points to v3/rna_ligand_3gca/audit/native,
not v4; an initial assumed v4 metadata lookup failed and was corrected from
the aggregate without launching a replacement job or changing old artifacts.

Job 596 continues the current-source panel with protein–DNA 7R6R, n=5,
native then FoldJAX, using the same frozen source and prior native XLA cache.
Output: `af3-current-7r6r-20260908-X82e6L/protein_dna_7r6r/audit`.
GPU was free before submission. This job is pending and does not yet expand
the three-case current-tree numerical coverage. Exact configuration gates
remain unchanged; no uninstrumented performance or release claim is added.

Job 596 completed both arms. Five 7R6R coordinate pairs and all raw confidence
leaves are bitwise equal; extracted confidence, input, parameter, RNG and
runtime/compiler checks pass. Entity maxima after fitting identical coordinates:
A 1.5720217060201747e-14, B 1.1688264056357194e-14,
D 1.0982263432883787e-14 angstrom. Only the two raw config-equality checks fail,
so driver exit 1 remains expected and is not an inference failure. Current-tree
numerical audit coverage is four cases / 20 paired samples, not the full panel.
Verification: completed comparison and both finished artifacts inspected;
no additional runtime modification, warm performance or release gate here.

Job 597 queues the three-component protein–RNA–ligand 3V7E audit, n=5,
under the same current source and recorded kernel policy. Output root:
`af3-current-3v7e-20260908-RheOJQ/protein_rna_ligand_3v7e/audit`.
Native and FoldJAX execute serially; the driver retains exact config checks.
Verification: GPU checked free and task queued. Numerical result pending;
current completed numerical coverage remains four cases / 20 sample pairs.

Job 597 completed both arms. All five 3V7E coordinates and raw confidence
leaves are bitwise equal. All input/parameter/RNG/runtime checks and extracted
confidence pass; only config.json/effective-config.json fail. Entity maxima
from fitting identical coordinates: L 4.180125496481331e-15,
P 2.4442717076183486e-14, R 1.7561066006793405e-14 angstrom.
Both arms logged the UFF atom-48 charge-state warning; independently captured
features and preprocessing tape still compare equal. This does not establish
universal chemical validity. Verification: both finished artifacts and report
inspected; exit 1 is the strict config comparator result. Current numerical
audit coverage is five cases / 25 paired samples; whole-panel closure remains open.

RNA–ligand 3GCA current-source paired audit is queued as job 598, using the
historical aggregate's v3 native XLA decisions and the same frozen source,
common runtime and n=5 settings as the other current checks. Output:
`af3-current-3gca-20260908-2Ru3pt/rna_ligand_3gca/audit`.
Verification: GPU checked free, submitted through tsp; numerical results
pending. No independent default-autotuning or performance claim is added.

Job 598 completed both 3GCA arms. Five coordinate pairs and raw confidence
leaves are bitwise equal; all non-config checks pass, including extracted
confidence, features, parameters, actual RNG and runtime/compiler provenance.
Fitted maxima: ligand L 4.35085119993769e-15 and RNA R
1.6099534198619772e-14 angstrom. Only config.json/effective-config.json fail,
so the driver exits 1 while inference completion is verified. Current-source
numerical audit coverage is six cases / 30 sample pairs. Remaining historical
panel cases are 7ST3, populated RNA MSA 9GMW and mapped template 9FM7; their
current-source results and uninstrumented bridges remain unverified.
Verification: both finished artifacts and completed comparison inspected.

Job 599 queues the current-source protein–protein 7ST3 pair (bucket768,
n=5), using that case's recorded Tokamax manifest and native XLA choices.
Output: `af3-current-7st3-20260908-BcMvch/protein_protein_7st3/audit`.
GPU was checked free; the native/FoldJAX pair executes serially through tsp.
Verification: submission complete, result pending. Six cases / 30 samples
remain the current completed numerical audit scope until this report finishes.

Job 599 finished both 7ST3 arms. All five coordinates and raw confidence
leaves are bitwise equal; all non-config checks pass. Fitted maxima for
identical coordinates are A 7.601760654448837e-14 and B
3.7028500115005564e-14 angstrom. Driver exit 1 reflects only the two raw config
checks. Verification: completed report and both finished artifacts inspected.
Current-source numerical audit coverage is seven cases / 35 sample pairs.

Job 600 queues populated RNA MSA 9GMW under the same frozen source and fixed
kernel settings, using the explicit historical input path from the run ledger.
Output `af3-current-9gmw-20260908-tlje3X/rna_9gmw_msa/audit`. The GPU was occupied
by 599 at submission, so 600 was serialized behind it. Result pending; no
current RNA-MSA result or uninstrumented performance claim is added yet.

Job 600 completed both populated-RNA-MSA 9GMW arms. Five coordinates and
raw confidence leaves are bitwise equal; all non-config checks pass, including
independent MSA features and observed RNG. Fitted A maximum is
9.722766801891299e-14 angstrom. Driver exit 1 reflects only the two raw config
checks. Verification: finished artifacts and full comparison inspected.
Current numerical audit coverage is eight cases / 40 sample pairs.

Job 601 queues the last historical case, mapped-template 9FM7 with duplicated
protein/ligand chains, using its explicit historical input and v5 native XLA
cache. Output `af3-current-9fm7-20260908-SejiNQ/template_9fm7_mapped/audit`.
GPU checked free at submission; same frozen source and n=5 settings retained.
Result pending; this is not yet the nine-case current-tree panel completion.

Job 601 completed both mapped-template 9FM7 arms. Five coordinates and all raw
confidence leaves are bitwise equal. All non-config checks pass, including
features, parameters, actual RNG and common-runtime/compiler provenance.
Entity fitted maxima: A 1.7806724786697046e-14, B 1.7592054881507103e-14,
L 2.1052765967583168e-14, M 2.18643004709253e-14 angstrom. Driver exit 1 is
solely the two raw config checks, not inference failure.

The current frozen source now has **nine audited cases / 45 sample pairs**
with exact coordinates and raw confidence, and passing all non-config checks.
This is not the historical 54-gate claim: the current-source uninstrumented
bridges/repeats and performance runs have not been repeated. Every current
raw config gate still records the two FoldJAX default-only fields as a
failure; there is no silent acceptance change. Publisher dependency-lock,
independent-autotuning, optional serving paths and release gates remain open.
Verification: all nine completed case reports were individually inspected;
the last pair has both finished artifacts. No new commit or push is claimed.

Current-source uninstrumented checks begin with job 602 (1UBQ, n=5, native
then FoldJAX, one warm repeat each). It uses the historical uninstrumented
native XLA cache, not an audit-only cache, and the same frozen current source.
Output `af3-current-perf-1ubq-20260908-kqyajM/protein_1ubq/performance`.
Verification: GPU checked free and job submitted via tsp. Output bridges,
first/warm equality and measured timings remain pending. One warm observation
per arm will be a baseline, not a statistically established speedup.

Job 602 completed both uninstrumented 1UBQ arms. Cross-arm comparison passes
every non-config check; driver exit 1 remains the two default-field config
differences. Recomputed output-only bridges from current audits 593/592 to
their own uninstrumented arms both pass with no failed checks. Both
`compare_first_warm` gates pass. These bridges do not grant RNG observation
to an uninstrumented run; RNG proof remains in the audit arms.

Measured first inference: native 35.006058427 s / FoldJAX 35.144360354 s.
Single warm inference: native 4.332936664 s / FoldJAX 4.331643661 s.
Allocator peak bytes: native 1,825,335,552 / FoldJAX 1,825,835,520.
These are single observations with compilation-inclusive process allocator
peaks, not statistically significant speedups or total physical VRAM.
Verification: completed pair report, both audit bridges, both first/warm
gates and saved timing records inspected. Other current uninstrumented panel
cases and release/push remain unverified.

Job 603 completed both current-source uninstrumented 5SAK arms under
`af3-current-perf-5sak-20260908-lmrvom/protein_ligand_5sak/performance`.
Cross-arm input, parameter, coordinates, raw/public confidence and compiler
provenance checks pass; the two raw config gates still fail and the driver
exits 1. This is not an inference failure or a full admission.
CPU recomputation of `compare_arms(..., require_tape=False)` from each arm of
`af3-current-5sak-20260908-mIDVLV/protein_ligand_5sak/audit` to its own
performance arm passes every check. `compare_first_warm` also passes for both
arms. Thus both 1UBQ and 5SAK now have current-source uninstrumented bridges
and first/warm checks; seven current-panel cases remain unchecked in this mode.
Verification: all four 5SAK bridge/repeat comparisons executed successfully
with `JAX_PLATFORMS=cpu .venv-ci/bin/python` and the current comparator.
Uninstrumented runs do not independently establish RNG-tape identity; the
observed-tape evidence remains in their audit counterparts. No runtime change,
tolerance relaxation, performance advantage, release, commit or push is claimed.

Job 604 completed current-source uninstrumented protein–RNA 1URN, n=5,
under `af3-current-perf-1urn-20260908-46nrPc/protein_rna_1urn/performance`.
It uses the same frozen source and fixed Tokamax policy, loading the historical
1URN performance native XLA cache. Cross-arm checks all pass except the two
raw config checks; driver exit 1 is retained. CPU comparisons against each
corresponding arm of `af3-current-1urn-20260908-DC5lGe/protein_rna_1urn/audit`
with `require_tape=False` pass, as do both first/warm repeat comparisons.
Current uninstrumented bridge/repeat coverage is now three of nine cases;
six remain, and full admission is still open.
Verification: four bridge/repeat comparisons pass; comparator regression
`JAX_PLATFORMS=cpu .venv-ci/bin/python -m pytest -q tests/test_af3_closure.py`
passes 42 tests. No numerical gate or model runtime was changed.

Job 605 completed current-source uninstrumented protein–DNA 7R6R, n=5,
under `af3-current-perf-7r6r-20260908-yRJMwQ/protein_dna_7r6r/performance`.
Cross-arm checks pass except the two raw config checks (driver exit 1).
Both output bridges from the corresponding arms of
`af3-current-7r6r-20260908-X82e6L/protein_dna_7r6r/audit` pass with
`require_tape=False`; both first/warm repeats pass. Current uninstrumented
bridge/repeat coverage is four of nine cases, not full admission.

Single-observation timing/memory records (native / FoldJAX):

| Case | First inference seconds | Warm seconds | Allocator peak bytes | Host max RSS KiB |
| --- | --- | --- | --- | --- |
| 1URN, job 604 | 35.079472 / 35.050147 | 4.285559 / 4.313861 | 1,825,327,360 / 1,825,631,488 | 7,064,148 / 7,071,740 |
| 7R6R, job 605 | 34.350917 / 33.494173 | 4.386705 / 4.419196 | 1,825,339,904 / 1,820,882,432 | 7,075,704 / 7,065,596 |

First inference includes compilation. Memory maxima are process-lifetime
measurements; allocator peak is not total physical VRAM. One warm observation
does not establish a speedup or a statistically reliable memory improvement.
Verification: four 7R6R bridge/repeat comparisons pass; timing fields read
directly from both cases' completed `finished.json` artifacts. No runtime or
acceptance criterion changed; review, remaining cases and release remain open.

Job 606 failed before inference because the submitted manifest path misspelled
`alphafold3`; this is a launch error, not model parity evidence. After checking
the correct file exists, job 607 completed both uninstrumented protein–RNA–ligand
3V7E arms, n=5, at
`af3-current-perf-3v7e-20260908-G5arHw/protein_rna_ligand_3v7e/performance`.
Every cross-arm check except the two raw config gates passes (driver exit 1).
Both output bridges from corresponding arms of
`af3-current-3v7e-20260908-RheOJQ/protein_rna_ligand_3v7e/audit` pass with
`require_tape=False`; both first/warm repeats pass. Current uninstrumented
bridge/repeat coverage is five of nine cases.

Native / FoldJAX first inference: 33.206419 / 33.524658 seconds; single warm:
4.398003 / 4.416806 seconds; process allocator peak: 1,825,844,480 /
1,825,330,432 bytes; host max RSS: 7,072,288 / 7,080,504 KiB. These retain the
same single-observation and compilation-inclusive memory limitations above.
Verification: all four bridge/repeat comparisons pass and both timing artifacts
were inspected. Full config admission, remaining cases, review and release
remain open; no runtime or tolerance change was made.

Job 608 failed before native outputs: strict loading of the historical v3
3GCA performance XLA cache lacked 14 of 452 required instruction configs.
The existing completed v4 performance cache was then selected, without
disabling complete-cache enforcement or extending cache entries. Job 609
completed both n=5 RNA–ligand arms at
`af3-current-perf-3gca-v4-20260908-Ry7iAK/rna_ligand_3gca/performance`.
Cross-arm checks pass except the two raw config checks (driver exit 1).
Both output bridges against corresponding arms of
`af3-current-3gca-20260908-2Ru3pt/rna_ligand_3gca/audit` pass with
`require_tape=False`; both first/warm repeats pass. This directly verifies
the cache selection did not introduce an output-bridge failure. Current
uninstrumented bridge/repeat coverage is six of nine cases.

Native / FoldJAX first inference: 32.924981 / 33.353283 seconds; single warm:
4.284931 / 4.311821 seconds; process allocator peak: 1,825,338,368 /
1,820,965,376 bytes; host max RSS: 7,057,400 / 7,058,672 KiB. These are single
observations, not a speedup or physical VRAM claim.
Verification: four bridge/repeat comparisons pass; both completed timing
artifacts inspected. Job 608 remains a recorded cache-coverage failure, not
a numerical comparison. Full config admission, remaining cases and release
are still open.

Job 610 completed current-source uninstrumented protein complex 7ST3, n=5,
at `af3-current-perf-7st3-20260908-GnogqH/protein_protein_7st3/performance`,
using the historical v4 performance cache and bucket-768 kernel manifest.
All cross-arm checks except the two raw config checks pass (driver exit 1).
Both output bridges against corresponding arms of
`af3-current-7st3-20260908-BcMvch/protein_protein_7st3/audit` pass with
`require_tape=False`; both first/warm repeats pass. Current uninstrumented
bridge/repeat coverage is seven of nine cases; RNA MSA and template remain.

Native / FoldJAX first inference: 54.421501 / 54.811431 seconds; single warm:
24.425462 / 24.742311 seconds; process allocator peak: 3,871,648,768 /
3,871,655,168 bytes; host max RSS: 7,386,168 / 7,379,012 KiB. Single observations
do not establish a performance advantage, and allocator peak is not physical
VRAM usage.
Verification: four bridge/repeat comparisons pass and both finished timing
artifacts inspected. Config admission, remaining cases and release stay open.

Job 611 completed current-source uninstrumented RNA MSA 9GMW, n=5, at
`af3-current-perf-9gmw-20260908-ZBsWKc/rna_9gmw_msa/performance`.
It retains the audit's explicit RNA MSA input and uses the historical v4
performance XLA cache. All cross-arm checks except the two raw config checks
pass (driver exit 1). Both output bridges against corresponding arms of
`af3-current-9gmw-20260908-tlje3X/rna_9gmw_msa/audit` pass with
`require_tape=False`; both first/warm repeats pass. Current uninstrumented
bridge/repeat coverage is eight of nine cases; the template case remains.

Native / FoldJAX first inference: 32.855869 / 33.580755 seconds; single warm:
4.334135 / 4.344451 seconds; process allocator peak: 1,820,888,320 /
1,821,128,448 bytes; host max RSS: 7,053,764 / 7,079,592 KiB. These single
observations are not evidence of a performance advantage.
Verification: four bridge/repeat comparisons pass and both completed timing
artifacts inspected. Config admission, template verification and release stay
open; no runtime or acceptance criterion was changed.

Job 612 completed current-source uninstrumented mapped-template 9FM7, n=5,
at `af3-current-perf-9fm7-20260908-6MSOI8/template_9fm7_mapped/performance`.
It retains the explicit template input and loads the historical v5 performance
XLA cache. All cross-arm checks except the two raw config gates pass (driver
exit 1). Both output bridges against corresponding arms of
`af3-current-9fm7-20260908-SejiNQ/template_9fm7_mapped/audit` pass with
`require_tape=False`; both first/warm repeats pass.

Current-source uninstrumented bridge/repeat coverage is now **nine of nine
cases**, with four passing bridge/repeat comparisons per case. Cross-arm raw
config equality remains false for every case; this is not full strict admission
or a replacement for the historical report below.

Native / FoldJAX first inference: 54.708832 / 55.095008 seconds; single warm:
24.692830 / 24.946890 seconds; process allocator peak: 3,884,658,688 /
3,884,575,744 bytes; host max RSS: 7,382,740 / 7,407,976 KiB. These are single
observations, not a performance advantage claim.
Verification: all four template bridge/repeat comparisons pass; both completed
timing artifacts inspected. Config admission, ordinary RNG evidence, publisher
dependency-lock scope, full regression/review and release remain open.

Current config discrepancy source audit: all nine completed performance pairs
were checked directly for both config files. Every native key/value is present
and equal in FoldJAX, no native-only key exists, and the only additional keys
are `foldjax_return_representations=[]` and `foldjax_stop_after="full"`.
In the carried model, the empty representation selection adds no output arrays;
`"full"` bypasses both early-return branches (`"inputs"` and `"trunk"`) and
executes diffusion and confidence normally. These are interface extensions at
their default full-prediction settings, not dtype/recycle/sampler mismatches.
This explanation is limited to these exact defaults; nondefault early stopping
is not upstream full-prediction parity. Raw config equality remains false and
the comparator is unchanged; a separate semantic-config policy needs explicit
review rather than silently deleting extra fields.
Verification: nine pairs / eighteen config files compared per arm; focused
representation and closure tests pass 49 tests with
`JAX_PLATFORMS=cpu .venv-ci/bin/python -m pytest -q
tests/models/alphafold3/test_representations.py tests/test_af3_closure.py`.
Representation unit tests use stubs; real-weight default-path evidence is the
nine-case audit and uninstrumented panel, not those stubs alone.

Independent policy consultation completed (OMS thread
`th-20260908T092717Z-2965299`). This was a description-only comparator-policy
review, not source inspection, real-weight verification or release approval.
The reviewer recommends a descriptive three-state config-difference result
(`none`, `allowlisted_extensions_only`, `other`) rather than a new semantic
parity PASS. Required safeguards include directional and exact-path allowlists,
strict recursive type equality, rejection of unknown/nondefault extensions,
malformed or missing evidence never counting as a pass, and tests locking the
allowlist itself. Parent confirmation: raw config equality currently does feed
`passed`, so every current cross-arm report remains false; the diagnostic must
not be OR-ed into that verdict. This scoped review does not close the source
or release review requirements. No comparator change was made in this step.

The descriptive config classifier is now implemented in `bench/af3_closure.py`.
It reports `none`, `allowlisted_extensions_only`, or `other`, without changing
the raw config checks or overall verdict. Recursive type-sensitive comparison
rejects bool/int and int/float confusion; extensions are top-level and directional.
The integration regression explicitly checks that an allowlisted extension
still leaves the overall comparison false.

Verification: all nine completed current performance pairs were recomputed on
CPU with the new comparator. Every pair reports only the two allowlisted
default extensions, passes all non-config checks, and retains `passed=false`.
Original saved comparison artifacts were not overwritten. Focused comparator
tests pass 61 tests. This diagnostic has no independent-source-review or
release-admission authority.

RNG scope clarification after inspecting the exact frozen capture source:
the AF3 audit observes each implementation's independently generated JAX draws
and checks key/value hashes; it does not inject reference noise into FoldJAX.
The `--reference` path compares independently generated input arrays and metadata,
not replaces them. In performance mode the `jax.random.normal` and MSA
`_padding_consistent_rng` observation patches are not installed. Thus the nine
performance pairs do use ordinary, unobserved inference RNG under the same
fixed seed and recorded kernel policy; they are not replay-injected samples.

However, performance mode still wraps NumPy RandomState and the RDKit conformer
boundary for preprocessing observations, as well as inference/output capture.
It is therefore incorrect to call this an entirely uninstrumented end-to-end
default CLI run. Independent kernel autotuning, additional seeds and a fully
observer-free preprocessing/CLI arm remain separate scope gaps. The observed
inference tape proof belongs to audit mode; the performance output bridge does
not itself observe its draws. Same-key warm repeats are repeatability tests,
not independent random replicates.
Verification: `cmp` confirms the inspected live capture file equals the frozen
capture used for the nine pairs; inference and preprocessing patch scopes and
the reference-input comparison were inspected directly. No new GPU job or
runtime change was needed to establish this reporting distinction.

Job 613 validates the new `--no-preprocessing-observers` performance option
on native 1UBQ, n=5, using a copy of the frozen source with only the updated
capture script: `af3-no-prep-source-20260908-YXqMPR`. Output is
`af3-no-prep-1ubq-20260908-FQBQWb/native`. The output-only comparison against
job 602 native passes every check, including inputs, parameters, coordinates
and raw/public confidence; first/warm repeat also passes. The saved preprocessing
record has `observers_enabled=false` and empty NumPy/conformer observations.
Verification: job 613 exits 0; both CPU comparisons pass and disabled-observer
artifacts are asserted. This is native-only evidence at one case, not a full
FoldJAX pair or panel. Inference/output capture and fixed kernel policy remain;
this still is not a completely unwrapped default CLI benchmark.

Job 614 completes the corresponding FoldJAX no-preprocessing-observers arm
at `af3-no-prep-1ubq-20260908-FQBQWb/foldjax`, with the same source as 613.
The output bridge against job 602 FoldJAX passes every check; first/warm repeat
passes. Comparing native 613 against FoldJAX 614 passes every non-config check,
retaining only the two known raw-config failures. The FoldJAX preprocessing
record explicitly disables observers and contains no draws/conformer records.
Verification: job 614 exits 0; both comparisons and repeat checks inspected on
CPU, with disabled-observer assertions. This closes the one-case preprocessing
observer-removal control, not the full observer-free panel or default CLI scope.

The [nine-case report](../bench/experiments/af3-closure-2026-09-06.json)
passes **54/54 gates**: audited pair, uninstrumented pair, both observer bridges
and both first/warm repeats for every case. All **45 sample pairs** have exactly
equal coordinates. Each case's 12 raw output leaves and every extracted
confidence leaf are **bitwise identical**, including matching undefined values.
Inputs, effective configuration, 405 loaded parameter leaves and actual audit
RNG events match independently; separate executable caches are used.

The following are per-entity-instance maxima over five samples, after the same
whole-system Kabsch fit in each sample. No entity is refitted or rematched.
Residuals below are floating-point roundoff from fitting identical coordinates,
not structural drift. All confidence maximum absolute differences are zero.

| Case | Composition / input feature | Bucket | Entity maximum RMSD (Å) |
| --- | --- | ---: | --- |
| 1UBQ | Protein | 256 | A: 6.42e-15 |
| 7R6R | Protein–DNA | 256 | A: 1.57e-14; B: 1.17e-14; D: 1.10e-14 |
| 5SAK | Protein–ligand | 512 | A: 1.99e-14; L: 1.11e-14 |
| 7ST3 | Protein–protein | 768 | A: 7.60e-14; B: 3.70e-14 |
| 1URN | Protein–RNA | 256 | P: 7.81e-15; R: 9.58e-15 |
| 3V7E | Protein–RNA–ligand | 256 | P: 2.44e-14; R: 1.76e-14; L: 4.18e-15 |
| 3GCA | RNA–ligand | 256 | R: 1.61e-14; L: 4.35e-15 |
| 9GMW | RNA with populated MSA | 256 | A: 9.72e-14 |
| 9FM7 | Two protein and two ligand copies, mapped template | 768 | A: 1.78e-14; B: 1.76e-14; L: 2.11e-14; M: 2.19e-14 |

The populated RNA MSA has 1,727 featurized alignments, before native selection
to depth 1,024, and 148,522 valid MSA positions. The template input maps 282
residues and has 13,536 valid template atom positions across its protein copies.
These are nonempty feature-path checks, not just accepted input keywords.

The [controls report](../bench/experiments/af3-closure-controls-2026-09-06.json)
also records a fresh **35/35 independent preprocessing** panel: 59 arrays and
five metadata fields per case, covering protein/DNA/RNA/CCD/SMILES/ions, all
two-kind combinations, mixed inputs, copies, modifications, disulfide/glycan
bonds and three MSA fixtures. This is prepared native-JSON preprocessing
coverage, not 35 full model-output cases or a database-search comparison.

## Measured performance and repeat controls

All values below are uninstrumented, five-sample native-BF16 predictions with
matched kernels. Each panel entry has one measured warm execution, so these
numbers establish a baseline, **not a statistically established speed or memory
improvement**. AF3 is already JAX upstream and these measurements are similar.
Parameter load/hash is outside inference timing. First-inference timings and
host peaks are retained in the machine-readable report.

| Case | Native warm (s) | FoldJAX warm (s) | Native allocator peak (GiB) | FoldJAX allocator peak (GiB) |
| --- | ---: | ---: | ---: | ---: |
| 1UBQ | 4.410 | 4.391 | 1.700 | 1.700 |
| 7R6R | 4.475 | 4.470 | 1.701 | 1.700 |
| 5SAK | 11.912 | 11.934 | 2.442 | 2.437 |
| 7ST3 | 25.019 | 25.094 | 3.606 | 3.604 |
| 1URN | 4.380 | 4.390 | 1.700 | 1.700 |
| 3V7E | 4.484 | 4.480 | 1.696 | 1.696 |
| 3GCA | 4.297 | 4.321 | 1.696 | 1.696 |
| 9GMW | 4.386 | 4.389 | 1.700 | 1.700 |
| 9FM7 | 25.239 | 25.276 | 3.606 | 3.606 |

Allocator peaks include parameter/compilation activity; they are not total
physical VRAM or reset warm-only live-tensor peaks. Hardware/runtime and cache
boundaries are recorded below and in the artifact manifests.

A separate fresh-executable 3GCA control retained five warm repetitions per
arm. Median latency was **4.4238 s native / 4.4447 s FoldJAX**; allocator peaks
were 1,825,338,624 / 1,820,660,992 bytes. Every repetition's raw output was
bitwise identical to its first inference; both independent-recompile bridges
and the cross-arm output gate passed. This is repeat stability with recorded
kernel decisions, not independent default-autotuning stability.

After the source correction, a separate **independently autotuned** 3GCA pair
still had equal inputs, parameters and actual RNG tape but differed numerically:

| Diagnostic maximum | Native vs independently autotuned native | Independently autotuned native vs FoldJAX |
| --- | ---: | ---: |
| RNA RMSD (Å) | 0.00292344 | 0.00792585 |
| Ligand RMSD (Å) | 0.00153808 | 0.00317666 |
| Atom pLDDT (points) | 0.198944 | 0.437531 |
| PAE (Å) | 0.108891 | 0.104197 |
| pTM | 0.000312104 | 0.000430180 |
| ipTM | 0.000472839 | 0.000505622 |

These coordinates pass 0.05 Å, but raw/extracted confidence fails the fixed
`atol=rtol=1e-4` diagnostic. Kernel-policy identity is intentionally absent in
this variability control. The same native source also varies; these results
must not be described as an isolated BF16-versus-FP32 error or as a completely
resolved default-autotuning discrepancy. No tolerance or default was changed.

## Verification and remaining scope

Verification: the final artifact aggregator exited 0 with nine passing cases
and 54 passing gates. All successful final GPU jobs were checked through exit 0,
including the corrected template audit/performance pair. Failed attempts remain
recorded below and in the controls report, not counted as successes.

The production correction passed the CPU CI command
`JAX_PLATFORMS=cpu CUDA_VISIBLE_DEVICES='' OMP_NUM_THREADS=2 .venv/bin/pytest -q
-m 'not network' --cov=foldjax --cov-report=term-missing --cov-fail-under=80`:
**3,664 passed, 399 skipped, 8 network tests deselected; 87.42% coverage**.
The final affected rerun passed **148 tests**, covering subsequent comparator
and reporter hardening. Syntax, repository-wide Ruff, affected format,
`uv lock --check` and Git diff checks passed.
The source-keyed AF3 runtime was rebuilt successfully; the publisher source
checkout was rechecked clean. No commit, push, remote CI or release was run.

The completed claim is limited to the common runtime and finite panel below.
Publisher-pinned dependency reproduction, fresh database searches, independent
C++/CCD builds, RDKit internal PRNG observation, other hardware/buckets and
default-autotuning exactness remain unverified. Neutral serving padding and
additional precision lowering are not admitted by this panel. No measured
optimization advantage or other-model closure is inferred from AF3's result.

## Fixed comparison profile

| Item | Both arms |
| --- | --- |
| Publisher | AlphaFold 3 v3.0.4, `85c4d20505fd5cef05eac22b534d4e793971ae69` |
| Reference runtime | Both arms use FoldJAX's installed runtime, not the publisher dependency lock |
| Hardware | Single NVIDIA RTX PRO 6000 Blackwell Max-Q, CUDA 13 |
| Compute | Native `global_config.bfloat16="all"`, preserving FP32 exceptions |
| Sampling | Five index-paired samples, 200 diffusion steps, 10 recycles (11 trunk passes) |
| MSA / batching | Native selection depth 1,024; original five-sample `hk.vmap`, no sample sharding |
| Matmul | Native `jax.default_matmul_precision=None`, no forced `highest` override |
| Inputs | Independently featurized native JSON, numeric and metadata exact |
| Shapes | Same publisher bucket grid, FoldJAX neutral padding disabled |
| Kernels | Triton attention; matched recorded Tokamax and XLA decisions for the parity gate |
| Coordinates | One proper all-valid-atom Kabsch per sample, RMSD per entity instance, no entity refit |
| Confidence | All raw heads and extracted fields; exact shape/dtype/order/masks and native NaN positions |

AF3 is already JAX upstream. This compares FoldJAX runtime integration and
retained patches, not a Torch rewrite. `bfloat16="all"` does not mean every
operation, parameter or coordinate is BF16. The numerical leaf diagnostic is
fixed at `atol=rtol=1e-4`; the coordinate diagnostic remains 0.05 Å per entity
in every sample. These are not a newly approved loss allowance for optional
precision lowering, whose structure/confidence criteria remain undecided.

The Tokamax autotune fallback is an explicit deviation from the publisher's
heuristic fallback: the local GPU has a documented shared-memory-incompatible
heuristic configuration. Both arms use the compatible policy. Generated CCD
assets and the C++ binary are shared and hashed; Python featurizers are separate.
This is not independent verification of two separate C++/CCD builds.

The native arm means **unmodified publisher Python executed on the common
FoldJAX runtime**, not an untouched publisher installation. The exact relevant
dependency differences are:

| Dependency | Publisher `pyproject.toml` pin | Both measured arms |
| --- | --- | --- |
| JAX / jaxlib | 0.10.2 (CUDA 12) | 0.11.1 (CUDA 13) |
| dm-haiku | 0.0.16 | 0.0.17 |
| Tokamax | 0.0.12 | 0.0.13 |
| RDKit | 2025.9.4 | 2026.3.5 |
| NumPy | Unpinned in `pyproject.toml` | 2.1.3 |

All measured runtime versions are bound into each provenance manifest. This
isolates FoldJAX's retained source/runtime-integration changes on its supported
environment. It does not establish that upgrading the publisher's dependency
lock leaves featurization, RNG, structure or confidence unchanged. A separate
publisher-locked environment comparison remains outside this closure claim;
the current project dependencies were not changed by this investigation.

Tokamax choices alone are insufficient: XLA also autotunes fused kernels.
The controlled producer records XLA decisions, and the consumer loads the
same decisions with `xla_gpu_require_complete_aot_autotune_results=true`.
JAX's automatic per-compilation-directory fusion cache is disabled for these
captures; compiled-executable caches remain separate. This follows the
[XLA persisted-autotuning mechanism](https://openxla.org/xla/persisted_autotuning).
No native compiled executable is copied into FoldJAX.

Removing observers changes 14 fusion keys in the 3GCA pilot. Strict loading of
only the audit cache correctly rejects that graph before outputs. The native
uninstrumented producer extends the cache for those new fusions; the bridge
checks hashes and preservation of every previous textproto record. The
uninstrumented FoldJAX consumer again requires complete cache coverage.
This does not waive coordinate or confidence gates. Independently autotuned
runs are a separate variability diagnostic, not this fixed-kernel proof.

## Actual randomness and measurement boundaries

The audit records actual JAX draw keys, dtypes, shapes and content hashes at
execution. It requires initial noise once, rotation/translation/churn 1,000
times each, and MSA Gumbel 11 times. It compares these event multisets exactly,
not just seed integers. This is an observed-tape identity check, not a serialized
noise array replay injector.

NumPy preprocessing normal/randint draws are recorded in consumption order,
including generator state identity. RDKit conformer seeds and returned
conformers are compared at the API boundary; RDKit's internal C++ PRNG stream
is not observable here. Final reference features must still match exactly.
Fixed-seed Fourier constants are covered by the independent source/runtime
identity, not counted as per-input sampler events.

Instrumented executions are not speed measurements. Separate uninstrumented
arms retain compile-inclusive first inference and synchronized warm inference,
including device/host output transfer. Their outputs must be compared both
against each other and against the corresponding audited outputs. A same-mode
repeat is also a separate runtime-stability control.
Parameter loading and hashing happen before the inference timer in the new
captures. They still contribute to process memory peaks. A compile-only speedup
must not be inferred from a producer that tunes versus a consumer that loads
already-recorded decisions. `--warm-repeats 5` additionally checks every repeated
raw output byte-for-byte outside the measured interval and reports a median.

Device memory is process allocator `peak_bytes_in_use`, including compilation/
autotuning activity; host memory is process `ru_maxrss`. Neither is a reset
warm-only live-tensor peak. Do not claim optimization benefits from audit times
or mix these definitions with unrelated measurements.

## Reproduction tools

`python -m bench.af3_closure_capture` accepts `native` or `foldjax`, plus
explicit `--input`, `--native-source`, `--weights` and a new `--out` directory.
The FoldJAX arm requires `--reference` pointing to the native capture and uses
the public prediction API. `--mode performance` disables the JAX observer and
retains first and repeated inference outputs. Queue GPU executions through tsp;
use immutable source snapshots and the run ledger. Never publish weights.
Run from the snapshot directory, not a live checkout: Python's `-m` lookup
places the working directory ahead of `PYTHONPATH`. The initial 3GCA fixed-XLA
pair used an unchanged, hashed live wrapper; later panel jobs use the actual
frozen directory as their working directory.

`python -m bench.af3_closure --left NATIVE --right FOLDJAX --out NEW_REPORT`
fails on missing evidence. `--output-only` is only the audited/unaudited output
bridge and explicitly grants no RNG admission.

`python -m bench.af3_closure_report --manifest CASE_PATHS_JSON
--artifact-base ARTIFACT_ROOT --out NEW_REPORT` aggregates six gates per case:
audit pair, uninstrumented pair, both observer bridges and both first/warm
repeats. It binds original artifact files and independent source-tree identities
by hash. Missing or failed gates cannot become a successful closure summary.

The initial pilot is RNA–ligand 3GCA. The completed finite output panel also
contains protein 1UBQ, protein–DNA 7R6R, protein–ligand 5SAK,
protein–protein 7ST3, protein–RNA 1URN and protein–RNA–ligand 3V7E.
The current panel also includes a populated RNA MSA (9GMW) and a nonempty
mapped template with ligand (9FM7). It remains finite, not all chemical inputs.
No crystal coordinates enter the parity measurements.
The 9FM7 input contains two 291-residue protein copies and two ligand copies;
its actual bucket is 768, not 512. The first template attempt mistakenly reused
the 512-token kernel manifest and failed strict Tokamax coverage before outputs.
Both that attempt and its dependent performance failure are retained. The retry
selects the already-prepared 768-token profile from actual feature shape, with
no input, dtype, schedule or tolerance change.

## Graph correction and compiler isolation

All 405 actually loaded parameter leaves matched in dtype, shape and bytes.
Without observers, native and carried StableHLO differed at only one operation:
native multiplied `noise_scale * noise`, whereas FoldJAX used `noise * noise_scale`.
The carried sampler now preserves native operand order. The entire resulting
StableHLO (excluding debug metadata) is byte-identical: 1,528,116 bytes, SHA256
`75723f6b90023765dfe9ad5a3fd56b8a96a6cae9b35eab6dbaf28b353b8013c0`.
The source-keyed AF3 runtime was rebuilt normally, including C++ and generated
assets; no cached runtime overlay was edited by hand.

This restores graph identity; it does **not** establish that commuting a
multiply alone caused all earlier coordinate/confidence drift. Earlier native
recompilations also differed. The full new control matches Tokamax **and** XLA
decisions, then compares independently executed native/public-FoldJAX arms.
The corrected 3GCA audit and uninstrumented bridge have exact coordinates and
raw/extracted confidence, including the first/warm repeat. No tolerance changed.

## Observer failure and control

The first full native capture aborted in XLA GPU fusion cost analysis before
outputs (`shape.IsArray() || shape.IsTuple()`, exit 134). A small AF3 sampler
control with a trivial denoiser reproduced the same failure with ordered
callbacks in seven seconds. The otherwise equivalent unordered-callback
control passed in eight seconds and observed all 3,001 sampler draws.

The observer was changed to unordered callbacks, retaining key/content event
identity. No AF3 arithmetic was changed. This isolates a compiler/observer
interaction, not a demonstrated model-port error. A subsequent native run
finished inference but failed its writer because the experimental manual C++
registration omitted the parent package attribute. The wrapper binding was
fixed, independently checked on CPU and rerun through successful output writing.
Failed lifecycle artifacts are preserved and never counted as closure.

## Historical pre-correction pilot checkpoint

This section preserves the earlier checkpoint. Its missing fixed-kernel panel,
uninstrumented and repeat evidence is supplied by the completed results above;
it is not the current status. Independent-autotuning variability and the wider
environment/chemistry exclusions remain separate as stated above.

[Machine-readable evidence](../bench/experiments/af3-closure-pilot-2026-09-06.json)
binds the input, source, wrapper, weights, CPP and output artifacts by hash.
All seven planned cases passed a fresh independent preprocessing check: 59
numeric/string arrays and five metadata fields per case matched exactly.
This is preprocessing coverage, not seven completed output comparisons.

3GCA's completed native and public FoldJAX audit arms matched actual JAX tape
(3,012 events), preprocessing draws/conformer boundaries, features and config.
Five samples contained 719 valid atoms each. Entity maxima use the one global
Kabsch transform, with no separate entity fit.

| Diagnostic | Completed native vs FoldJAX | Native fixed-Tokamax control vs FoldJAX |
| --- | ---: | ---: |
| RNA maximum RMSD (Å) | 0.04078223 | 0.03550325 |
| Ligand maximum RMSD (Å) | 0.01138981 | 0.01217634 |
| Maximum atom pLDDT difference (points) | 0.8988800 | 0.5263138 |
| Maximum PAE difference (Å) | 0.5298805 | 0.4236803 |
| Maximum pTM difference | 0.00129060 | 0.00103294 |
| Maximum ipTM difference | 0.00088249 | 0.00055681 |

Coordinates pass the recorded diagnostic; raw and extracted confidence fail
the fixed leaf diagnostic. The two independently compiled native inference
runs also differ (RNA 0.02046766 Å, ligand 0.00280204 Å, pLDDT 0.6163712
points maximum); the first of those had a failed writer but retained its raw
inference and actual tape. This control is not two successful full lifecycles.

The fixed-kernel control reapplied the completed FoldJAX run's 19 Tokamax
configurations to native and failed on uncovered configurations rather than
retuning. It did not eliminate the discrepancy. It fixes Tokamax choices,
**not every XLA/cuBLAS compiler choice**; the remaining cause is unresolved.
This is not evidence that the AF3 arithmetic port is wrong, nor proof that all
differences are explained by autotuning. No tolerance was relaxed.

The earlier FoldJAX capture predates the expanded runtime-version manifest.
It uses the same local environment, but the newer strict artifact gate correctly
flags that missing field; it must not be silently backfilled as captured data.
Future admission needs complete runtime manifests on both arms.

Historical verification: 121 affected CPU tests passed; Ruff and diff checks passed.
The observer controls and the completed native/FoldJAX/fixed-kernel audit jobs
were checked through their final exit status. No production AF3 arithmetic or
dtype change, commit, push or release is claimed.

At that historical checkpoint, the remaining six cases' output/confidence
comparisons, matched uninstrumented controls, first/warm repeat and performance/
memory measurements were missing. These were prerequisites to expanding the
panel, not permission to call the earlier pilot complete.
