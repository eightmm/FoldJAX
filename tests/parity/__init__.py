"""The CPU-runnable parity regression subset.

Package (rather than a bare test directory) so the fetch helper is reachable as
``python -m tests.parity.fetch``, which is the command the fixture resolver
prints when a fixture is missing.
"""
