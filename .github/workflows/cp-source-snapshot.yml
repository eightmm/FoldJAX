name: CP source snapshot

on:
  pull_request:

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: foldjax-cp-source
          path: |
            .
            !.git
          include-hidden-files: true
          retention-days: 1
