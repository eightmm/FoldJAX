"""Vendored JAX model implementations.

Each subpackage is a full, self-contained inference port that FoldJAX
drives through ``foldjax.backends``. They keep their upstream module
layout so the ports stay diffable against their source repositories.
"""
